name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd docare/staging/DocareWeb
            git pull
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S curl -sL https://deb.nodesource.com/setup_16.x | sudo -E bash -
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S apt-get install -y nodejs
            npm install
            npm run build
            cp -rf ./build/* ~/public_html/docarestaging